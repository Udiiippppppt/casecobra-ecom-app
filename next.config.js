/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint:{
    ignoreDuringBuilds:true,
  },
}



module.exports = nextConfig, {
  
    images: {
      domains: ['utfs.io'], // Add the external image domain here
    },
  };
  